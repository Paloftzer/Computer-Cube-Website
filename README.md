Images and stylesheets should be placed in the static directory otherwise they won't work properly.

To easily install all needed modules for the project to run, you can run the command "pip install -r requirements.txt" and it will automatically install all modules.